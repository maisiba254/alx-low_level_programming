#include <stdio.h>
/**
*main - Entry point
*Description: prints single digits of base 10
*Return: 0 Always success
*/
int main(void)
{
int num;

for (num = 0; num <= 9; num++)
printf("%d\n", num);
return (0);
}
