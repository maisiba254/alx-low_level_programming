#include <stdio.h>
/**
*main - Entry point
*
*Description: alphabets in small letters
*
*Return: 0 Always success
*
*/

int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	putchar(ch);
	putchar('\n');
return (0);
}
